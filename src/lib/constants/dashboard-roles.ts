import type { DashboardRole } from '@/types/dashboard-widgets';

/**
 * Liste de tous les rôles dashboard (centralisé)
 */
export const ALL_DASHBOARD_ROLES: DashboardRole[] = ['direction', 'manager', 'agent', 'admin'];

